export enum Projection {
    Sum = "SUM",
    Max = "MAX",
    Min = "MIN",
    Avg = "AVG",
    Count = "COUNT",
    Cast = "CAST",
    Distinct = "DISTINCT",
    Round = "ROUND",
    Coalesce = "COALESCE",
    BetweenParenthesis = "",
}
